<%= bootstrap_form_for @form, layout: :horizontal, label_col: 'col-sm-2', control_col:  'col-sm-8'  do |f| %>
  <%= f.text_field :name %>
  <%= f.text_area :description %>
  <%= f.form_group do %>
    <button type="submit" class="btn btn-success">
      <i class="fa fa-plus"></i> Create
    </a>
  <% end %>
<% end %>
