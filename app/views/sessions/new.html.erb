<div class="container">
  <div class="sign-in-form">
    <%= bootstrap_form_for User.new, url: sessions_path, layout: :horizontal  do |f| %>
      <%= f.text_field :email %>
      <%= f.password_field :password %>
      <%= f.form_group do %>
        <button type="submit" class="btn btn-success">
          <i class="fa fa-sign-in"></i> Create
        </a>
      <% end %>
    <% end %>
  </div>
</div>
